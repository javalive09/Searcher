<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimary_"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <EditText
            android:id="@+id/search"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="@string/search"
            android:imeOptions="actionDone"
            android:layout_marginRight="@dimen/search_padding_right"
            android:cursorVisible="false"
            android:textColor="@color/edit_text_no_focus"
            android:focusable="false"
            android:singleLine="true"/>

        <TextView
            android:id="@+id/opt_multi"
            android:layout_width="@dimen/multi_h"
            android:layout_height="@dimen/multi_h"
            android:layout_gravity="right|center_vertical"
            android:gravity="center"
            android:textColor="@color/color_top_grap"
            android:layout_marginRight="@dimen/multi_padding"
            android:textSize="10sp"
            android:textStyle="bold"
            android:text="1"
            android:onClick="onClick"
            android:background="@drawable/opt_multi_selector"/>

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="@dimen/top_h"
            android:background="@color/title_bg"
            android:text="速搜"
            android:gravity="center_vertical"
            android:textSize="17sp"
            android:visibility="gone" />
    </FrameLayout>
</android.support.v7.widget.Toolbar>